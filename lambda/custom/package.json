{
  "name": "hello-world-alexa-template",
  "version": "1.0.0",
  "description": "Hello world Alexa skill template",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "dependencies": {
    "alexa-sdk": "^1.0.0"
  }
}
